# Ferramentas

DETRAN MG: Registrar Intenção de Venda de Veículo expõe 1 ferramenta (todas somente leitura).

### 1. `detran_mg_reg_intencao_venda_consultar`
**Input**: `cpf_vendedor` (opcional), `cnpj_vendedor` (opcional), `email_vendedor`, `placa`, `chassi`, `renavam`, `crv`, `hodometro` (opcional), `datahora_hodometro` (opcional), `valor_venda`, `cpf_comprador` (opcional), `cnpj_comprador` (opcional), `nome_comprador`, `email_comprador`, `rg_comprador` (opcional), `cep_comprador`, `logradouro_endereco_comprador`, `numero_endereco_comprador`, `complemento_endereco_comprador` (opcional), `bairro_endereco_comprador`, `municipio_endereco_comprador`, `uf_endereco_comprador`

DETRAN MG: Registrar Intenção de Venda de Veículo, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DETRAN MG: Registrar Intenção de Venda de Veículo
```
